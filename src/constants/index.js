const X = 'X';
const O = 'O';
const NONE = 'NONE';

const DEFAULT_BOARD = [
    NONE, NONE, NONE,
    NONE, NONE, NONE,
    NONE, NONE, NONE
];
const DEFAULT_TURN = X;

const UPDATE_BOARD = 'UPDATE_BOARD';

export default {
    X,
    O,
    NONE,
    DEFAULT_BOARD,
    DEFAULT_TURN,
    UPDATE_BOARD
};
